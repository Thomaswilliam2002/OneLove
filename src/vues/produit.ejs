<!DOCTYPE html>
<html lang="en">

  
<!-- Mirrored from buybootstrap.com/demos/medflex/medflex-admin-dashboard/doctors-list.html by HTTrack Website Copier/3.x [XR&CO'2014], Thu, 15 May 2025 12:16:08 GMT -->
<%- include('head')%>

  <body>

    <!-- Page wrapper starts -->
    <div class="page-wrapper">

      <!-- App header starts -->
      <% if(user.Personnel.type_personnel === 'admin'){%>
        <%- include('header')%>
      <% }else{%>
        <%- include('header-staff')%>
      <% }%>
      <!-- App header ends -->

      <!-- Main container starts -->
      <div class="main-container">

        <!-- Sidebar wrapper starts -->
        <% if(user.Personnel.type_personnel === 'admin'){%>
          <%- include('menu')%>
        <% }else{%>
          <%- include('menu-staff')%>
        <% }%>
        <!-- Sidebar wrapper ends -->

        <!-- App container starts -->
        <div class="app-container">

          <!-- App hero header starts -->
          <div class="app-hero-header d-flex align-items-center">

            <!-- Breadcrumb starts -->
            <ol class="breadcrumb">
              <li class="breadcrumb-item">
                <i class="ri-home-8-line lh-1 pe-3 me-3 border-end"></i>
                <a href="index.html">Accueil</a>
              </li>
              <li class="breadcrumb-item text-primary" aria-current="page">
                Liste des Produit
              </li>
            </ol>
            <!-- Breadcrumb ends -->

            <!-- Sales stats starts -->
            
            <!-- Sales stats ends -->

          </div>
          <!-- App Hero header ends -->

          <!-- App body starts -->
          <div class="app-body">

            <!-- Row starts -->
            <div class="row gx-3">
              <div class="col-sm-12">
                <div class="card">
                  <div class="card-header d-flex align-items-center justify-content-between">
                    <h5 class="card-title">Liste des Produit</h5>
                    <a href="/formAddProduit" class="btn btn-primary ms-auto">Ajouter un nouveau produit</a>
                  </div>
                  <div class="card-body">
                      <% if(msg) {%>
                        <% if(type === 'achat') {%>
                          <% if(msg === 'ajout') {%>
                            <div class="alert bg-white border alert-dismissible fade show text-success" role="alert">
                              <b>Transaction </b> ajouter avec succes !
                              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                          <% }%>
                        <% }%>
                        <% if(type === 'vente') {%>
                          <% if(msg === 'ajout') {%>
                            <div class="alert bg-white border alert-dismissible fade show text-success" role="alert">
                              <b>Transaction </b> ajouter avec succes !
                              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                          <% }%>
                        <% }%>
                        <% if(type === 'article') {%>
                          <% if(msg === 'ajout') {%>
                            <div class="alert bg-white border alert-dismissible fade show text-success" role="alert">
                              <b>Produit </b> ajouter avec succes !
                              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                          <% }%>
                          <% if(msg === 'modif') {%>
                            <div class="alert bg-white border alert-dismissible fade show text-info" role="alert">
                              <b>Produit </b> modifier avec succes !
                              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                          <% }%>
                          <% if(msg === 'sup') {%>
                            <div class="alert bg-white border alert-dismissible fade show text-danger" role="alert">
                              <b>Produit </b> supprimer avec succes !
                              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                          <% }%>
                        <% }%>
                      <% }%>
                    <!-- Table starts -->
                    <div class="table-responsive">
                      <table id="customButtons" class="table truncate m-0 align-middle">
                        <thead>
                          <tr class="text-center">
                            <th>ID</th>
                            <th>Nom du Produit</th>
                            <th>Quantiter</th>
                            <th>seuil de rappel</th>
                            <th>Categorie</th>
                            <th>Description</th>
                            <th>Actions</th>
                          </tr>
                        </thead>
                        <tbody>
                          <%produits.forEach((produit, index) => {const matricule = String(index + 1).padStart(4, "0");%> 
                            
                          <tr class="text-center">
                            <td><%= matricule %></td>
                            <td>
                              <%= produit.nom %>
                            </td>
                            <td><%= produit.quantiter %></td>
                            <td><%= produit.seuil %></td>
                            <% categories.forEach(categorie => { if(produit.id_categ === categorie.id_categ){ %>
                              <td><%= categorie.nom %></td>
                            <% }})%>
                            <td><%= produit.description %></td>
                            <td>
                              <div class="d-inline-flex gap-1">
                                
                                  <!-- Card starts -->
                                      <!-- Button trigger modal -->
                                      <button type="button" data-bs-toggle="modal"
                                        data-bs-target="#delete<%= produit.id_produit %>ModalCenter" class="btn btn-outline-danger btn-sm">
                                        <i class="ri-delete-bin-line"></i>
                                      </button>
                                      <!-- Modal -->
                                      <div class="modal fade" id="delete<%= produit.id_produit %>ModalCenter" tabindex="-1"
                                        aria-labelledby="personnel.id_personnelModalCenterTitle" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                          <div class="modal-content">
                                            <div class="modal-header">
                                              <h5 class="modal-title" id="delete<%= produit.id_produit %>ModalCenterTitle">
                                                Confirmer
                                              </h5>
                                              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                            </div>
                                            <div class="modal-body">
                                              <p>Voulez-vous vraiment supprimer l'article: <%= produit.nom %> ?</p>
                                            </div>
                                            <div class="modal-footer">
                                              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                                annuler
                                              </button>
                                              <form action="/deleteProduit/<%= produit.id_produit %>?_method=DELETE" method="post">
                                                <div class="col-sm-12">
                                                  <div class="d-flex gap-2 justify-content-end">
                                                    <button type="submit" class="btn btn-danger">Supprimer</button>
                                                  </div>
                                                </div>
                                              </form>
                                            </div>
                                          </div>
                                        <!-- </form> -->
                                        </div>
                                      </div>
                                  <!-- Card ends -->
                                
                                <!-- <a href="edit-doctors.html" class="btn btn-outline-success btn-sm"
                                  data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="Edit Doctor Details">
                                  <i class="ri-edit-box-line"></i>
                                </a> -->
                                <!-- ----------------------------------------------------------------------------------- -->
                                <!-- Card starts -->
                                <!-- Button trigger modal -->
                                <button type="button" data-bs-toggle="modal"
                                  data-bs-target="#update<%= produit.id_produit %>ModalCenter" class="btn btn-outline-success btn-sm">
                                  <i class="ri-edit-box-line"></i>
                                </button>
                                <!-- Modal -->
                                <div class="modal fade" id="update<%= produit.id_produit %>ModalCenter" tabindex="-1"
                                  aria-labelledby="personnel.id_personnelModalCenterTitle" aria-hidden="true">
                                  <div class="modal-dialog modal-dialog-centered">
                                    <div class="modal-content">
                                      <div class="modal-header">
                                        <h5 class="modal-title" id="update<%= produit.id_produit %>ModalCenterTitle" style="white-space: normal; word-wrap: break-word; overflow: break-word;">
                                          Modification 
                                        </h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                      </div>
                                      <div class="modal-body">
                                        <form action="/updateProduit/<%= produit.id_produit %>?_method=PUT" method="post">
                                          <div class="row gx-3">
                                            <div class="col-sm-12">
                                              <div class="bg-light rounded-2 px-3 py-2 mb-3">
                                                <h6 class="m-0">Details de l'Article</h6>
                                              </div>
                                            </div>
                                            <div class="col-xxl-3 col-lg-6 col-sm-6">
                                              <div class="mb-3">
                                                <label class="form-label" for="a1">Nom de l'article<span class="text-danger">*</span></label>
                                                <input type="text" class="form-control" id="a1" value="<%= produit.nom %>" placeholder="Entrer le nouveau nom" name="nom" required>
                                              </div>
                                            </div>
                                            <div class="col-xxl-3 col-lg-6 col-sm-6">
                                              <div class="mb-3">
                                                <label class="form-label" for="a2">Categorie<span class="text-danger">*</span></label>
                                                <select class="form-select" id="dep" name="categ">
                                                  <% if(categories){ categories.forEach(categorie => {%>
                                                      <option value="<%= categorie.id_categ %>"><%= categorie.nom %></option>
                                                  <% })}else{ %>
                                                    <option value="null">Aucune categorie disponinble</option>
                                                  <% } %>
                                                </select>
                                              </div>
                                            </div>
                                            <div class="col-xxl-3 col-lg-6 col-sm-6">
                                              <div class="mb-3">
                                                <label class="form-label" for="a1">Seuil de rappel<span class="text-danger">*</span></label>
                                                <input type="text" class="form-control" id="a1" value="<%= produit.seuil %>" placeholder="Entrer le nouveau nom" name="seuil" required>
                                              </div>
                                            </div>
                                            <div class="col-xxl-3 col-lg-8 col-sm-6">
                                              <div class="mb-3 form-check">
                                                  <label class="form-label" for="a">Lappel par email<span class="text-danger">*</span></label>
                                                  <input type="checkbox" class="form-check-input" value="1" name="rappel" id="a">
                                              </div>
                                            </div>
                                            <div class="col-sm-12">
                                              <div class="mb-3">
                                                <label class="form-label" for="a8">Description <span class="text-danger">*</span></label>
                                                <textarea class="form-control" id="a8" rows="2" name="desc"><%= produit.description %></textarea>
                                              </div>
                                            </div>
                                          </div>
                                          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                            annuler
                                          </button>
                                          <button type="submit" class="btn btn-danger">Appliquer les modification</button>
                                        </form>
                                      </div>
                                    </div>
                                  <!-- </form> -->
                                  </div>
                                </div>
                                  <!-- Card ends -->
                                <!-- ----------------------------------------------------------------------------------- -->
                                <a href="/oneProduit/<%= produit.id_produit %>" class="btn btn-outline-info btn-sm"
                                  data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="Historique de l'article">
                                  <i class="ri-eye-line"></i>
                                </a>
                                <!-- -------------------------------------------------------------------------------------------------- -->
                                <!-- <a href="doctors-profile.html" class="btn btn-outline-info btn-sm"
                                  data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="Augmenter le nombre en stock">
                                  <i class="ri-file-reduce-line"></i>
                                </a> -->
                                <!-- Card starts -->
                                <!-- Button trigger modal -->
                                <button type="button" class="btn btn-outline-info btn-sm" data-bs-toggle="modal"
                                  data-bs-target="#<%= produit.id_produit %>exampleModalCenter" data-bs-title="Augmenter le nombre en stock">
                                  <i class="ri-file-reduce-line"></i>
                                </button>
                                <!-- Modal -->
                                <div class="modal fade" id="<%= produit.id_produit %>exampleModalCenter" tabindex="-1"
                                  aria-labelledby="<%= produit.id_produit %>exampleModalCenterTitle" aria-hidden="true">
                                  <div class="modal-dialog modal-dialog-centered">
                                    <div class="modal-content">
                                      <div class="modal-header">
                                        <h5 class="modal-title" id="<%= produit.id_produit %>exampleModalCenterTitle">
                                          Modiffication apres Sortie de Produit
                                        </h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                      </div>
                                      <div class="modal-body">
                                        <!-- Row starts -->
                                        <% if (produit.quantiter >= 1){ %>
                                        <form action="/addHSortie/<%= produit.id_produit %>?art=produit" method="post">
                                          <div class="row gx-3">
                                            <div class="col-sm-12">
                                              <div class="bg-light rounded-2 px-3 py-2 mb-3">
                                                <h6 class="m-0">Details de la Vente</h6>
                                              </div>
                                            </div>
                                            
                                            <div class="col-xxl-3 col-lg-6 col-sm-6">
                                              <div class="mb-3">
                                                <label class="form-label" for="a1">Quantiter/Nombre a retirer<span class="text-danger">*</span></label>
                                                <input type="number" min="1"  max="<%= produit.quantiter %>" step="0.01" class="form-control" id="a1" placeholder="Entrer le nombre ou la quqntiter" name="nbr" required>
                                              </div>
                                            </div>
                                            <div class="col-xxl-3 col-lg-6 col-sm-6">
                                              <div class="mb-3">
                                                <label class="form-label" for="a2">Prix a l'unité <span class="text-danger">*</span></label>
                                                <input type="number" min="0" step="0.01" class="form-control" id="a2" placeholder="Entrer le prix unitaire" name="prix" required>
                                              </div>
                                            </div>
                                            <div class="col-sm-12">
                                              <div class="mb-3">
                                                <label class="form-label" for="a8">Nom du destinataire<span class="text-danger">*</span></label>
                                                <input list="dest" class="form-control"  placeholder=" Ajouter le nom l'entité ou de la personne qui est destinataire" name="dest" required>
                                                <!-- <input type="hidden" class="form-control" id="dest_id" placeholder=" Ajouter le nom l'entité ou de la personne qui est destinataire" name="dest" required> -->
                                                <datalist id="dest">
                                                  <% if(barSimples){ barSimples.forEach(barSimple => {%>
                                                    <option value="<%= barSimple.nom %>" data-id="<%= barSimple.id_barSimple %>" ></option>
                                                  <% }) }%>
                                                  <% if(barVips){ barVips.forEach(barVip => {%>
                                                    <option value="<%= barVip.nom %>" data-id="<%= barVip.id_barVip %>"></option>
                                                  <% }) } %>
                                                  <% if(crazycs){ crazycs.forEach(crazyc => {%>
                                                    <option value="<%= crazyc.nom %>" data-id="<%= crazyc.id_cclub %>"></option>
                                                  <% }) } %>
                                                </datalist>
                                              </div>
                                            </div>
                                            <div class="col-sm-12">
                                              <div class="mb-3">
                                                <label class="form-label" for="a8">Commantaire<span class="text-danger">*</span></label>
                                                <textarea class="form-control" id="a8" placeholder="un commantaire ?" rows="2" name="cmm"></textarea>
                                              </div>
                                            </div>
                                            <input type="hidden" class="form-control" id="a2" name="type" value="produit">
                                            <input type="hidden" class="form-control" id="a2" name="idpro" value="<%= produit.id_produit %>">
                                          </div>
                                          <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                              Annuler
                                            </button>
                                            <button type="submit" class="btn btn-danger">Confirmer le retrait</button>
                                          </div>
                                        </form>
                                          <!-- <button type="button" class="btn btn-primary" data-bs-dismiss="modal">
                                            Confirmer le retrait
                                          </button> -->
                                      </div>
                                      <% } else { %>
                                        <div class="alert bg-white border alert-dismissible fade show text-danger" role="alert">
                                          <b>Imposssible </b> de diminuer le stock ! <br>
                                          Actuellement le stock est a <b>0</b> .
                                        </div>
                                      <% } %>
                                    </div>
                                  </div>
                                </div>
                                <!-- Card ends -->
                              </div>
                                <!-- -------------------------------------------------------------------------------------------------- -->
                                <!-- <a href="doctors-profile.html" class="btn btn-outline-info btn-sm"
                                  data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="Reduire le nombre en stock">
                                  <i class="ri-file-add-line"></i>
                                </a> -->

                                <button type="button" class="btn btn-outline-info btn-sm" data-bs-toggle="modal"
                                  data-bs-target="#<%= produit.id_produit %>exampleModalCenter<%= produit.id_produit %>" data-bs-title="Di minuer le nombre en stock">
                                  <i class="ri-file-add-line"></i>
                                </button>
                                <!-- Modal -->
                                <div class="modal fade" id="<%= produit.id_produit %>exampleModalCenter<%= produit.id_produit %>" tabindex="-1"
                                  aria-labelledby="<%= produit.id_produit %>exampleModalCenterTitle<%=produit.id_produit %>" aria-hidden="true">
                                  <div class="modal-dialog modal-dialog-centered">
                                    <div class="modal-content">
                                      <div class="modal-header">
                                        <h5 class="modal-title" id="<%= produit.id_produit %>exampleModalCenterTitle<%= produit.id_produit %>">
                                          Modiffication apres Entrer de Produit
                                        </h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                      </div>
                                      <div class="modal-body">
                                        <!-- Row starts -->
                                        <form action="/addHEntrer/<%= produit.id_produit %>?art=produit" method="post">
                                        <div class="row gx-3">
                                          <div class="col-sm-12">
                                            <div class="bg-light rounded-2 px-3 py-2 mb-3">
                                              <h6 class="m-0">Details de l'ajout</h6>
                                            </div>
                                          </div>
                                          <div class="col-xxl-3 col-lg-6 col-sm-6">
                                            <div class="mb-3">
                                              <label class="form-label" for="a1">Quantiter/Nombre a ajouter<span class="text-danger">*</span></label>
                                              <input type="number" min="1" step="0.01" class="form-control" id="a1" placeholder="Entrer le nombre ou la quantiter" name="nb" required>
                                            </div>
                                          </div>
                                          <div class="col-xxl-3 col-lg-6 col-sm-6">
                                            <div class="mb-3">
                                              <label class="form-label" for="a2">Prix a l'unité <span class="text-danger">*</span></label>
                                              <input type="number" min="0" step="0.01" class="form-control" id="a2" placeholder="Entrer le prix unitaire" name="prix" required>
                                            </div>
                                          </div>
                                          <input type="hidden" class="form-control" id="a2" name="type" value="produit">
                                          <input type="hidden" class="form-control" id="a2" name="idpro" value="<%= produit.id_produit %>">
                                      </div>
                                      </div>
                                      <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                                          Annuler
                                        </button>
                                        <button type="submit" class="btn btn-success">Confirmer l'ajout</button>
                                      </form>
                                        <!-- <button type="button" class="btn btn-primary" data-bs-dismiss="modal">
                                          Confirmer l'ajout
                                        </button> -->
                                      </div>
                                    </div>
                                  </div>
                                </div>
                                <!-- ----------------------------------------------------------------------------------- -->
                                <!-- <div class="icons">
                                  <i class="ri-file-reduce-line"></i>
                                </div>
                                <div class="icons">
                                  <i class="ri-file-add-line"></i>
                                </div> -->
                              </div>
                            </td>
                          </tr>
                          <% }) %>
                        </tbody>
                      </table>
                    </div>
                    <!-- Table ends -->

                    <!-- Modal Delete Row -->
                    
                  </div>
                </div>
              </div>
            </div>
            <!-- Row ends -->
            <!-- ---------------------------------------------------------------- -->
             <br>
            <div class="">
              <div class="" style="background-color: rgb(215, 230, 225);">
                <div class="card-body">
                  <div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="false">
                    <div class="carousel-indicators">
                      <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="0"
                        class="active" aria-current="true" aria-label="Slide 1"></button>
                      <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="1"
                        aria-label="Slide 2"></button>
                      <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="2"
                        aria-label="Slide 3"></button>
                    </div>
                    <div class="carousel-inner pb-3" id="pp">
                    </div>

                    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators"
                      data-bs-slide="prev" >
                      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    </button>
                    <button class="carousel-control-next " type="button" data-bs-target="#carouselExampleIndicators"
                      data-bs-slide="next" >
                      <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    </button>

                  </div>
                </div>
              </div>
            </div>
             <!-- ---------------------------------------------------------------- -->
          </div>
          <!-- App body ends -->
          
          <!-- App footer starts -->
          <div class="app-footer bg-white">
            <span>© OneLove admin 2026</span>
          </div>
          <!-- App footer ends -->

        </div>
        <!-- App container ends -->

      </div>
      <!-- Main container ends -->

    </div>
    <!-- Page wrapper ends -->

    <!-- *************
			************ JavaScript Files *************
		************* -->
    <script src="assets/vendor/apex/apexcharts.min.js"></script>
    <%- include('script')%>
    <script>
        
      const text_colore = ['text-primary', 'text-info', 'text-danger', 'text-warning', 'text-success']
      const class_colore = ['badge bg-primary', 'badge bg-info', 'badge bg-danger', 'badge bg-warning', 'badge bg-success']
      traduct = (date) => {
          const dates = new Date(`${date.split('-')[0]}-${date.split('-')[1]}-01`);
          const shortMonth = dates.toLocaleString('en-US', {month: 'short'});
          return shortMonth;
      }
      
      const pp = document.getElementById('pp');
      const anner = []
      

      const recette = JSON.parse('<%- JSON.stringify(sumhes) %>')
      console.log('r', recette)

      recette.forEach(element => {
        if(!anner.includes(element.mois.split('-')[0])){
          anner.push(element.mois.split('-')[0])
        }
      });

      console.log(anner)

      // ----------------------------------------------------------------------
      anner.forEach((anner, ia) => {
        const nb_bs = []
        const nom = []
        const all_total = []

        

        recette.forEach(element => {
          if(element.mois.split('-')[0] === anner){
            if(!nb_bs.includes(element.id_probal)){
              nb_bs.push(element.id_probal)
            }
            if(!nom.includes(element.nom)){
              nom.push(element.nom)
            }
          }
        });


        for(let i = 0; i < nb_bs.length; i++){
          let total = 0
          recette.forEach((element, index) => {
            if(nb_bs[i] === element.id_probal && anner === element.mois.split('-')[0]){
              total = total + element.total_recette
            }
          });
          all_total.push(total);
        }

        console.log('=>',nb_bs, all_total)

        pp.insertAdjacentHTML('beforeend',`
          <div class="card-body carousel-item ${ia === 0? 'active' : ''}" id="p${ia}">
          </div>
        `)

        const p = document.getElementById(`p${ia}`);

        boole = 0
        nb_bs.forEach((id, index) => {

          const all_month = ["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec",]
          const recettes = Array(12).fill(0);
          recette.forEach(element => {
            if(element.id_probal === id && anner === element.mois.split('-')[0]){
              for(let j = 0; j < all_month.length; j++){
                if(all_month[j] === traduct(element.mois)){
                  //console.log(element.total_recette, typeof element.total_recette)
                  recettes[j] = element.total_recette
                }
              }
            }
          })
          var options = {
            chart: {
              height: 300,
              type: "line",
              toolbar: {
                show: true,
              },
            },
            title:{
              text: `${nom[index]}: ${anner}`,
              aligne: 'left',
              style:{
                color: `red`,
              }
            },
            dataLabels: {
              enabled: false,
            },
            stroke: {
              curve: "smooth",
              width: 3,
            },
            series: [
              {
                name: "Recette",
                data: recettes,
              },
            ],
            grid: {
              borderColor: "rgb(99, 226, 184)",
              strokeDashArray: 5,
              xaxis: {
                lines: {
                  show: true,
                },
              },
              yaxis: {
                lines: {
                  show: true,
                },
              },
              padding: {
                top: 0,
                right: 0,
                bottom: 10,
                left: 0,
              },
            },
            xaxis: {
              categories: all_month,
            },
            yaxis: {
              labels: {
                show: true,
              },
            },
            colors: ["#116aef", "#ce313e", "#436ccf", "#dcad10", "#828382"],
            markers: {
              size: 0,
              opacity: 0.3,
              colors: ["#116aef", "#ce313e", "#436ccf", "#dcad10", "#828382"],
              strokeColor: "#ffffff",
              strokeWidth: 1,
              hover: {
                size: 7,
              },
            },
            tooltip: {
              y: {
                formatter: function (val) {
                  return val + "FCFA";
                },
              },
            },
          };

          exi = document.getElementById(`#income${ia}`);
          console.log(exi)
          if(!exi){
            p.insertAdjacentHTML('beforeend',`
              <div class="chart-height-lg">
                  <div id="income${ia}" ></div>
              </div>
            `)
          }

          console.log(id, anner)


          const hot = document.getElementById(`income${ia}`);
          hot.insertAdjacentHTML('beforeend',`
            <div class="" id="d${boole}-${anner}">
            </div>
          `)
          
          var chart = new ApexCharts(document.querySelector(`#d${boole}-${anner}`), options);

          chart.render();
          boole++
        })
      })
    </script>
    <script>
      // const dest_label= document.getElementById("dest_label");
      // const dest_id = document.getElementById("dest_id");
      // dest_label.addEventListener("input", () => {
      //   const selectOption = Array.from(dest_label.list.options).find(option => option.value === dest_label.value);
      //   if(selectOption){
      //     dest_id.value = selectOption.getAttribute("data-id")
      //   }
      // })

    </script>
  </body>


<!-- Mirrored from buybootstrap.com/demos/medflex/medflex-admin-dashboard/doctors-list.html by HTTrack Website Copier/3.x [XR&CO'2014], Thu, 15 May 2025 12:16:08 GMT -->
</html>